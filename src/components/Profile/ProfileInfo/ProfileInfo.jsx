import React from "react";
import s from "./ProfileInfo.module.css";

const ProfileInfo = (props) => {
  return (
      <div className={s.descriptionBlock}>ава + описание</div>
  );
};

export default ProfileInfo;
